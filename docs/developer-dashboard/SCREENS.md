# Screens

Single route: `DeveloperDashboardScreen`

- Material 3 **dark** engineering theme (`DevDashColors`)
- Responsive: packet monitor + event log side-by-side ≥1100px
- Modular section widgets inside the screen file (no business logic in widgets)

Providers: `developerDashboardProvider` (`Notifier`)
