/// Application-level configuration placeholders.
///
/// Business values and environment wiring will be added in later modules.
class AppConfig {
  const AppConfig._();

  static const String appName = 'NeedForNeeds';
  static const String packageName = 'com.needforneeds';
}
