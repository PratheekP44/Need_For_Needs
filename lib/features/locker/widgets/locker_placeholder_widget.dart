import 'package:flutter/material.dart';

/// Shared widget placeholder for the Locker feature.
class LockerPlaceholderWidget extends StatelessWidget {
  const LockerPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
