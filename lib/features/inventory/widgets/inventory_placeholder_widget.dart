import 'package:flutter/material.dart';

/// Shared widget placeholder for the Inventory feature.
class InventoryPlaceholderWidget extends StatelessWidget {
  const InventoryPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
