import 'package:flutter/material.dart';

/// Shared widget placeholder for the Orders feature.
class OrdersPlaceholderWidget extends StatelessWidget {
  const OrdersPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
