import 'package:flutter/material.dart';

/// Shared widget placeholder for the Payment feature.
class PaymentPlaceholderWidget extends StatelessWidget {
  const PaymentPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
