import 'package:flutter/material.dart';

/// Shared widget placeholder for the Admin feature.
class AdminPlaceholderWidget extends StatelessWidget {
  const AdminPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
